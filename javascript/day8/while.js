// initalization of counter
let secondCounter = 0;

// condition for loop
while (secondCounter < 60) {
  console.log(secondCounter);

  // counter for loop
  secondCounter = secondCounter + 1;
}

// WAP to print n numbers of even numbers.
